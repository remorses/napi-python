[package]
name = "test-addon"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.16", features = ["napi9", "tokio_rt"] }
napi-derive = "2.16"
tokio = { version = "1", features = ["time"] }

[build-dependencies]
napi-build = "2.1"
